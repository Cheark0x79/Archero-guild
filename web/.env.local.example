# Next.js server routes read the same database URL.
# Copy this to web/.env.local for local dashboard DB mode.
ARCHERO_DATABASE_URL=postgresql://archero:archero_dev_password@127.0.0.1:55440/archero_observer
ARCHERO_PUBLIC_ORIGIN=http://localhost:5181

# DATABASE_URL is also supported. ARCHERO_DATABASE_URL has priority when both exist.
# DATABASE_URL=postgresql://archero:archero_dev_password@127.0.0.1:55440/archero_observer

# Authentication is required in development too.
ARCHERO_ADMIN_PASSWORD=replace-with-a-local-password
ARCHERO_ADMIN_SESSION_TOKEN=replace-with-a-random-local-session-token
